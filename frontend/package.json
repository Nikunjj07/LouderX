{
  "name": "sydney-events-frontend",
  "version": "1.0.0",
  "description": "Sydney Events Aggregator Frontend",
  "scripts": {
    "start": "live-server --port=3000 --no-browser",
    "build": "echo 'No build needed for static site'",
    "deploy": "echo 'Deploy to Vercel'"
  },
  "keywords": ["events", "sydney", "aggregator"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "env": {
    "API_URL": "http://localhost:5000/api"
  }
}